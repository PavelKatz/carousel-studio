# Render-engine dependencies — regenerated by `npm run setup`
scripts/carousel-render/node_modules/
scripts/carousel-render/.cache/

# OS / misc
.DS_Store
*.log

# Local project data created when USING the plugin (not part of the plugin)
carousel.config.json
carousel/
