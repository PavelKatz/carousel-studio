---
description: Собрать Instagram-карусель из идеи, текста или ссылки
argument-hint: <тема | вставленный текст | ссылка>
---

Собери Instagram-карусель из входа ниже, используя агента `carousel-creator` (делегируй через Agent tool, `subagent_type: carousel-creator`).

Если `carousel.config.json` в проекте нет — сначала предложи запустить `/carousel-setup`.

Вход от пользователя:

$ARGUMENTS
